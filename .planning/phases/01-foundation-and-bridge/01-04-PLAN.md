---
phase: 01-foundation-and-bridge
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/app/app.routes.ts
  - frontend/src/app/components/landing/landing.component.ts
  - frontend/src/app/components/landing/landing.component.html
autonomous: true
must_haves:
  truths:
    - Landing page now served at /login instead of /
    - / route is reserved for future cinematic intro
    - /door and /world routes exist (components created in future plans)
    - All existing authenticated routes still work with AuthGuard
    - Wildcard redirect sends unknown paths to /login
    - GameComponent lazy-loaded so /dashboard users don't pay Phaser bundle cost
  artifacts:
    - frontend/src/app/app.routes.ts: Updated routing with new structure
    - frontend/src/app/components/landing/: Landing component moved to /login route
  key_links:
    - /login → LandingComponent (existing auth flow)
    - / → reserved for cinematic (placeholder redirect to /login for now)
    - /world → lazy-loaded GameComponent (Phaser bundle only loads when needed)
    - AuthGuard → still protects /dashboard and existing routes
---

<objective>
Restructure application routing to support game flow while preserving existing authentication and dashboard functionality.
Purpose: Create route architecture that supports cinematic intro → door → world game flow while ensuring Phaser bundle is lazy-loaded.
Output: Updated app.routes.ts with /login for landing, placeholder routes for /, /door, /world, and lazy-loading for game module.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move landing page to /login route</name>
  <files>frontend/src/app/app.routes.ts</files>
  <action>
  Read current app.routes.ts to understand existing routing structure.

  Update the routes array:

  1. Find the current '/' route (likely pointing to LandingComponent)
  2. Change that route's path from '' to 'login'
  3. Keep all properties the same (component, title, etc.)

  Example transformation:
  FROM:
  ```typescript
  { path: '', component: LandingComponent, title: 'Platform' }
  ```

  TO:
  ```typescript
  { path: 'login', component: LandingComponent, title: 'Platform - Login' }
  ```

  Ensure imports remain correct and no other changes to existing routes yet.
  </action>
  <verify>
  - TypeScript compiles without errors
  - LandingComponent route now at 'login' path
  - All other routes unchanged
  - No duplicate route definitions
  </verify>
  <done>
  Landing page accessible at /login instead of /
  </done>
</task>

<task type="auto">
  <name>Task 2: Add game routes with lazy-loading</name>
  <files>frontend/src/app/app.routes.ts</files>
  <action>
  Add new routes to app.routes.ts:

  1. Add / route as temporary redirect (will be cinematic later):
  ```typescript
  {
    path: '',
    redirectTo: '/login',
    pathMatch: 'full'
  }
  ```

  2. Add /world route with lazy-loading:
  ```typescript
  {
    path: 'world',
    loadComponent: () => import('./game/game.component').then(m => m.GameComponent),
    title: 'Platform - World'
  }
  ```

  3. Add placeholder /door route (component doesn't exist yet):
  ```typescript
  {
    path: 'door',
    redirectTo: '/login', // Temporary until door component created
    pathMatch: 'full'
  }
  ```

  4. Ensure wildcard redirect exists at end of routes array:
  ```typescript
  {
    path: '**',
    redirectTo: '/login'
  }
  ```

  Place these routes BEFORE any wildcard route but AFTER the login route.
  Maintain all existing authenticated routes (dashboard, etc.) unchanged.
  </action>
  <verify>
  - TypeScript compiles without errors
  - Route order: '', 'login', 'door', 'world', ...existing routes..., '**'
  - /world uses loadComponent for lazy-loading
  - Build succeeds: `cd frontend && npx ng build`
  - In build output, verify separate chunk created for game.component
  </verify>
  <done>
  Routes configured with / → /login redirect, /world lazy-loaded, /door placeholder, wildcard fallback
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify lazy-loading isolation</name>
  <files>frontend/src/app/app.routes.ts</files>
  <action>
  After build, verify Phaser bundle is isolated:

  1. Run production build:
  ```bash
  cd frontend && npx ng build --configuration production
  ```

  2. Check dist/browser/ for chunk files:
  ```bash
  ls -lh dist/browser/*.js | grep -i game
  ```

  3. Verify that:
     - A separate game component chunk exists (e.g., game-component-HASH.js)
     - This chunk is NOT loaded when visiting other routes
     - The chunk contains Phaser code (~1.2MB)

  4. Document findings:
     - Note the chunk file name pattern
     - Confirm size includes Phaser (~1.2MB+)
     - Verify main bundle doesn't include Phaser

  No code changes needed - this task is verification only.
  If lazy-loading doesn't work (Phaser in main bundle), report error for debugging.
  </action>
  <verify>
  - Separate JS chunk exists for game component
  - Chunk size is significantly larger (~1.2MB+) due to Phaser
  - Main bundle size doesn't include Phaser overhead
  - TypeScript build completes successfully
  </verify>
  <done>
  Production build shows GameComponent and Phaser code in separate lazy-loaded chunk
  </done>
</task>

</tasks>

<verification>
1. Run `cd frontend && npx ng build` - completes without errors
2. Navigating to / redirects to /login
3. /login shows LandingComponent (existing auth flow)
4. /world route exists and lazy-loads GameComponent
5. /door redirects to /login temporarily
6. Unknown routes redirect to /login via wildcard
7. Existing authenticated routes (/dashboard, etc.) still work
8. Build output shows separate chunk for game component
</verification>

<success_criteria>
1. Landing page moved from / to /login successfully
2. / route redirects to /login (placeholder for future cinematic)
3. /world route lazy-loads GameComponent (Phaser not in main bundle)
4. /door route exists as placeholder redirect
5. Wildcard route catches unknown paths
6. All existing AuthGuard-protected routes unchanged and functional
7. Production build creates separate chunk for game code containing Phaser
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-bridge/01-04-SUMMARY.md`
</output>
