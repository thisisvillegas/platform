---
phase: 02-game-world
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/systems/WorldLoader.ts
  - frontend/src/app/game/scenes/BootScene.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement tilemap loading and rendering system that loads Tiled JSON maps and renders multiple layers with correct ordering.
Purpose: Display the village world with proper ground, buildings, decorations, and above-player layers.
Output: WorldLoader.ts system that loads Tiled JSON and renders all map layers in OverworldScene.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorldLoader system for Tiled JSON maps</name>
  <files>frontend/src/app/game/systems/WorldLoader.ts</files>
  <action>
    Create WorldLoader.ts that:
    - Loads Tiled JSON map format (orthogonal, 16x16 tiles)
    - Parses tilemap layers array (ground, buildings, decorations, collision, above-player)
    - Loads and attaches tileset images referenced in map JSON
    - Supports multiple tileset files per map
    - Creates Phaser tilemap and tileset objects
    - Returns configured tilemap ready for rendering
    - Handles layer visibility and depth ordering
  </action>
  <verify>Load a test Tiled JSON map, verify tilemap object is created with all layers</verify>
  <done>WorldLoader successfully loads Tiled JSON and creates Phaser tilemap with all layers parsed</done>
</task>

<task type="auto">
  <name>Task 2: Integrate tilemap rendering in BootScene and OverworldScene</name>
  <files>frontend/src/app/game/scenes/BootScene.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    In BootScene:
    - Preload tilemap JSON (placeholder path: assets/worlds/village/maps/village.json)
    - Preload tileset images referenced in map
    In OverworldScene:
    - Use WorldLoader to load tilemap
    - Render layers in correct order: ground, buildings, decorations, (player sprite), above-player
    - Set above-player layer depth to render over player sprite
    - Position map at world origin
  </action>
  <verify>Run game, tilemap renders with all layers visible in correct z-order</verify>
  <done>Village tilemap renders with proper layer ordering, above-player elements render over player sprite</done>
</task>

</tasks>

<verification>
Load game and verify tilemap renders. Walk player sprite behind a tree or building roof marked in above-player layer to confirm depth ordering works.
</verification>

<success_criteria>
1. Tiled JSON map loads successfully
2. All tileset images attach correctly
3. Ground, buildings, decorations render below player
4. Above-player layer renders over player sprite
5. Multiple tilesets work in same map
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-02-SUMMARY.md`
</output>
