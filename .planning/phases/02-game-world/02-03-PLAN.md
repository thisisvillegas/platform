---
phase: 02-game-world
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/app/game/systems/CameraController.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement camera system that smoothly follows the player, clamps to world bounds, and provides cinematic arrival zoom effect.
Purpose: Create professional game feel with smooth camera tracking and polished scene transitions.
Output: CameraController.ts with smooth follow, world bounds clamping, and configurable zoom.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CameraController with smooth follow and bounds</name>
  <files>frontend/src/app/game/systems/CameraController.ts</files>
  <action>
    Create CameraController.ts that:
    - Implements smooth camera follow using lerp interpolation
    - Configurable lerp speed (default 0.1 for smooth tracking)
    - Clamps camera to map bounds (no void showing beyond edges)
    - Calculates bounds based on map dimensions and camera viewport
    - Provides setBounds() method for different map sizes
    - Provides setTarget() to change follow target
    - Update method called each frame to smooth follow player
  </action>
  <verify>Move player around map, camera follows smoothly and stops at map edges</verify>
  <done>Camera smoothly tracks player with no jitter and clamps correctly to world bounds</done>
</task>

<task type="auto">
  <name>Task 2: Add arrival zoom effect and zoom level configuration</name>
  <files>frontend/src/app/game/systems/CameraController.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Extend CameraController to:
    - Support configurable zoom levels (default 2x for pixel art)
    - Implement arrival zoom effect: start at 0.5x zoom (bird's-eye), tween to 2x over 2 seconds
    - Use Phaser camera zoom tweens with easing (Cubic.Out)
    - Trigger arrival effect on scene start
    - Provide setZoom() method for runtime zoom changes
    Integrate in OverworldScene:
    - Initialize CameraController with player as target
    - Set camera bounds to map dimensions
    - Trigger arrival zoom on scene create
  </action>
  <verify>Enter OverworldScene, camera starts zoomed out and smoothly zooms to game level over 2 seconds</verify>
  <done>Arrival zoom effect plays on scene start, camera supports configurable zoom levels</done>
</task>

</tasks>

<verification>
Load game and watch arrival zoom effect. Move player to map edges to verify camera bounds. Player should always stay visible and camera should not show void.
</verification>

<success_criteria>
1. Camera smoothly follows player with lerp interpolation
2. Camera clamps to map bounds on all edges
3. Arrival zoom effect plays from bird's-eye to game zoom
4. Zoom levels are configurable
5. No jitter or snapping in camera movement
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-03-SUMMARY.md`
</output>
