---
phase: 02-game-world
plan: 04
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - frontend/src/app/game/systems/CollisionSystem.ts
  - frontend/src/app/game/systems/WorldLoader.ts
  - frontend/src/app/game/systems/PlayerController.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement collision detection system that prevents player from walking through buildings, obstacles, and map boundaries.
Purpose: Create realistic world boundaries and prevent player from walking through solid objects.
Output: CollisionSystem.ts that parses Tiled collision layer and enforces physics-based collision with player.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CollisionSystem that parses Tiled collision layer</name>
  <files>frontend/src/app/game/systems/CollisionSystem.ts, frontend/src/app/game/systems/WorldLoader.ts</files>
  <action>
    Create CollisionSystem.ts that:
    - Parses "collision" layer from Tiled map
    - Identifies collision tiles (any tile value > 0 in collision layer)
    - Sets Phaser tile collision properties on tilemap layer
    - Creates collision bodies for static obstacles
    - Provides enableCollision() method that accepts player sprite
    - Sets up Phaser physics collider between player and collision layer
    Update WorldLoader.ts to:
    - Return reference to collision layer
    - Mark collision layer as invisible (not rendered)
  </action>
  <verify>Load map with collision layer, verify collision tiles are detected and marked</verify>
  <done>Collision layer is parsed and collision properties are set on appropriate tiles</done>
</task>

<task type="auto">
  <name>Task 2: Enable player collision and map bounds constraint</name>
  <files>frontend/src/app/game/systems/PlayerController.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Update PlayerController.ts to:
    - Enable physics body on player sprite
    - Set physics body size to match sprite collision box
    - Constrain player to world bounds using Phaser world bounds
    In OverworldScene:
    - Initialize CollisionSystem with loaded tilemap
    - Enable collision between player and collision layer
    - Set world bounds to map dimensions
    - Enable player world bounds collision
    - Ensure player cannot exit map area
  </action>
  <verify>Try to walk player through buildings and map edges, player should be blocked</verify>
  <done>Player collides with buildings, obstacles, and cannot walk beyond map boundaries</done>
</task>

</tasks>

<verification>
Walk player toward buildings, trees, water, and map edges. Player should stop when colliding with any obstacle or boundary.
</verification>

<success_criteria>
1. Player cannot walk through buildings marked in collision layer
2. Player cannot walk through trees, water, or other obstacles
3. Player is constrained to map bounds
4. Collision detection is smooth with no glitching
5. Collision layer is invisible but functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-04-SUMMARY.md`
</output>
