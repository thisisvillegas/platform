---
phase: 02-game-world
plan: 05
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - frontend/src/app/game/systems/InteractionSystem.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement proximity-based interaction system with visual prompts and dual input support (keyboard + mobile tap).
Purpose: Enable players to interact with NPCs, doors, and items using keyboard or touch.
Output: InteractionSystem.ts with proximity detection, interaction prompts, and callback system.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InteractionSystem with proximity detection and prompts</name>
  <files>frontend/src/app/game/systems/InteractionSystem.ts</files>
  <action>
    Create InteractionSystem.ts that:
    - Tracks interactive objects (NPCs, doors, items) loaded from Tiled object layer
    - Implements proximity detection (configurable radius, default 32 pixels)
    - Shows interaction prompt sprite/text ("Press Enter") when player is near object
    - Hides prompt when player moves away
    - Manages prompt positioning above interactive object
    - Provides registerInteractable() method to add objects
    - Provides update() method to check proximity each frame
    - Returns nearest interactable object when in range
  </action>
  <verify>Walk player near registered interactable, prompt appears and disappears based on distance</verify>
  <done>Interaction prompts appear when player is near objects and disappear when moving away</done>
</task>

<task type="auto">
  <name>Task 2: Add interaction input handling and callback system</name>
  <files>frontend/src/app/game/systems/InteractionSystem.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Extend InteractionSystem to:
    - Listen for Enter/Space key press for keyboard interaction
    - Handle mobile tap events on interactive objects
    - Trigger interaction callback when input detected and object in range
    - Support object-specific callbacks (different actions per object type)
    - Provide setCallback() method to register interaction handlers
    - Pass interacted object data to callback
    In OverworldScene:
    - Parse Tiled object layer for door zones (building entrances)
    - Register door zones as interactables with InteractionSystem
    - Set callback for door interactions (placeholder: log building ID)
    - Create test interactable for verification
  </action>
  <verify>Walk to door zone, prompt appears, press Enter or tap, callback fires with object data</verify>
  <done>Keyboard and mobile tap trigger interactions, callbacks receive correct object data</done>
</task>

</tasks>

<verification>
Walk player to building doors. Prompt should appear, Enter key or tap should trigger interaction callback. Test on both desktop and mobile.
</verification>

<success_criteria>
1. Proximity detection shows/hides prompts correctly
2. Enter/Space key triggers interaction when in range
3. Mobile tap on objects triggers interaction
4. Interaction callbacks receive correct object data
5. Multiple object types supported (doors, NPCs, items)
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-05-SUMMARY.md`
</output>
