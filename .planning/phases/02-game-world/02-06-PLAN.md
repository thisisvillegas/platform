---
phase: 02-game-world
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - frontend/src/app/game/scenes/BootScene.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/app/game/scenes/InteriorScene.ts
  - frontend/src/app/game/systems/SceneTransition.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement scene management system with fade transitions, data passing, and player position persistence.
Purpose: Enable smooth transitions between overworld and building interiors with state preservation.
Output: SceneTransition.ts and updated scenes (Boot, Overworld, Interior) with transition effects and data passing.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SceneTransition system with fade effects</name>
  <files>frontend/src/app/game/systems/SceneTransition.ts</files>
  <action>
    Create SceneTransition.ts that:
    - Provides fadeOut() method that creates black rectangle overlay
    - Animates overlay alpha from 0 to 1 over configurable duration (default 500ms)
    - Provides fadeIn() method that animates overlay alpha from 1 to 0
    - Supports callback on fade completion
    - Provides transitionTo() method that: fadeOut, switch scene, fadeIn
    - Passes scene data object to target scene
    - Cleans up overlay after transition
  </action>
  <verify>Call transitionTo() between scenes, verify smooth fade out/in effect</verify>
  <done>Scene transitions include smooth fade effects with configurable duration</done>
</task>

<task type="auto">
  <name>Task 2: Implement BootScene, OverworldScene, InteriorScene with data passing</name>
  <files>frontend/src/app/game/scenes/BootScene.ts, frontend/src/app/game/scenes/OverworldScene.ts, frontend/src/app/game/scenes/InteriorScene.ts</files>
  <action>
    Update BootScene:
    - Show loading progress bar for asset preloading
    - Transition to OverworldScene on complete
    Update OverworldScene:
    - Accept scene data from transitions (returnFromInterior flag, savedPlayerPos)
    - Save player position when entering building
    - Use SceneTransition to switch to InteriorScene
    - Pass building ID and player position to InteriorScene
    - Restore player position when returning from interior
    Create InteriorScene:
    - Accept scene data (buildingId, returnPlayerPos)
    - Load building-specific interior map based on buildingId
    - Provide exit interaction that returns to OverworldScene
    - Pass saved player position back to OverworldScene
  </action>
  <verify>Enter building, transition to interior with fade. Exit building, return to overworld at same position.</verify>
  <done>Scene transitions work bidirectionally with player position preserved across scenes</done>
</task>

</tasks>

<verification>
Load game through BootScene with progress bar. Enter a building, verify fade transition to InteriorScene. Exit building, verify return to OverworldScene at saved position.
</verification>

<success_criteria>
1. BootScene shows loading progress and transitions to OverworldScene
2. Fade out/in effects play smoothly on all transitions
3. Entering building passes buildingId to InteriorScene
4. Player position is saved when entering building
5. Exiting building restores player to saved position
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-06-SUMMARY.md`
</output>
