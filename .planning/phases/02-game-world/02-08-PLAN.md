---
phase: 02-game-world
plan: 08
type: execute
wave: 5
depends_on: [02-07]
files_modified: []
autonomous: false
must_haves:
  truths: []
  artifacts:
    - Village overworld map JSON (Tiled export)
    - Building interior maps JSON (4 interiors)
  key_links:
    - https://www.mapeditor.org/ (Tiled Map Editor)
---

<objective>
Design village overworld and building interior maps in Tiled map editor with all required layers.
Purpose: Create the playable village world with 10 buildings, proper collision, and interactive zones.
Output: Exported Tiled JSON maps for village overworld and 4 building interiors.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Design village overworld map in Tiled</name>
  <files>frontend/src/assets/worlds/village/maps/village.json</files>
  <action>
    Create new orthogonal map in Tiled:
    - Tile size: 16x16 pixels
    - Map size: 80x60 tiles (or larger as needed)
    - Import village-tileset.png as tileset
    Create 6 tile layers (bottom to top):
    1. ground (grass, dirt, paths)
    2. buildings (building walls, bases)
    3. decorations (flowers, fences, signs)
    4. collision (mark impassable tiles)
    5. above-player (tree canopies, building roofs)
    Add object layers:
    - door-zones: rectangles at building entrances (name property = buildingId)
    - npc-spawns: points for NPC positions (name property = npcId)
    - collectibles: points for item positions
    - player-spawn: single point for player start position
    Place 10 buildings: tavern, smart-home, war-room, greenhouse, theater, castle, workshop, server-shack, about-house, generic
    Export as JSON to frontend/src/assets/worlds/village/maps/village.json
  </action>
  <verify>Open exported JSON, verify all layers and object layers are present</verify>
  <done>Village overworld map designed with 10 buildings and all required layers, exported as JSON</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Design building interior maps</name>
  <files>frontend/src/assets/worlds/village/maps/interiors/</files>
  <action>
    Create 4 interior maps in Tiled:
    1. tavern-interior.json (bar, tables, NPCs)
    2. generic-lobby.json (reusable for multiple buildings)
    3. server-shack-interior.json (server racks, terminal)
    4. about-house-interior.json (personal space, decorations)
    Each map:
    - Tile size: 16x16 pixels
    - Map size: 20x15 tiles (smaller than overworld)
    - Same 6 layers: ground, buildings, decorations, collision, above-player
    - Object layer: exit-zone (rectangle at door, triggers return to overworld)
    - Use same village-tileset.png
    Export all to frontend/src/assets/worlds/village/maps/interiors/
  </action>
  <verify>Open each interior JSON, verify exit-zone object exists</verify>
  <done>4 building interior maps designed and exported as JSON</done>
</task>

</tasks>

<verification>
Verify all exported JSON files are valid and contain expected layers and object layers. Check file sizes are reasonable (not empty).
</verification>

<success_criteria>
1. Village overworld map contains 10 distinct buildings
2. All 6 tile layers present in overworld map
3. Door zones object layer has 10 entries with buildingId names
4. Player spawn point defined in object layer
5. 4 interior maps exported with exit zones
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-world/02-08-SUMMARY.md`
</output>
