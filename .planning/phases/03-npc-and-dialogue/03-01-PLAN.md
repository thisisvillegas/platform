---
phase: 03-npc-and-dialogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/managers/NPCManager.ts
  - frontend/src/app/game/entities/NPC.ts
  - frontend/src/app/game/scenes/VillageScene.ts
autonomous: true
must_haves:
  truths:
    - NPC-01: Claude NPC stands in town square with idle animation
    - NPC-02: Walking near an NPC shows interaction prompt ("Press SPACE to talk")
  artifacts:
    - NPCManager.ts class that loads NPCs from Tiled object layer
    - NPC entity class with idle animation and proximity detection
    - Interaction prompt sprite or text above NPC when player is nearby
  key_links:
    - "@.planning/ROADMAP.md Phase 3 success criteria"
---

<objective>Build NPC system with spawning, idle behavior, and interaction prompts
Purpose: Visitors can see the Claude NPC in the town square and know they can interact when nearby
Output: NPCManager.ts, NPC.ts, and VillageScene integration with working proximity detection</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create NPCManager with Tiled object layer loading</name>
  <files>frontend/src/app/game/managers/NPCManager.ts</files>
  <action>
    Create NPCManager.ts that:
    - Loads NPC spawn positions from Tiled map object layer named "NPCs"
    - Parses NPC properties (name, sprite, dialogueId) from Tiled objects
    - Stores NPC instances in a Map keyed by ID
    - Provides getClosestNPC(playerX, playerY, maxDistance) method
    - Handles NPC lifecycle (spawn, update, destroy)
  </action>
  <verify>NPCManager can parse Tiled object data and instantiate NPCs at correct positions</verify>
  <done>NPCManager.ts exists with complete spawn and proximity detection logic</done>
</task>

<task type="auto">
  <name>Task 2: Create NPC entity with idle animation and collision</name>
  <files>frontend/src/app/game/entities/NPC.ts</files>
  <action>
    Create NPC.ts class that:
    - Extends Phaser.GameObjects.Sprite or Container
    - Takes spawn position, sprite key, name, and dialogueId in constructor
    - Plays idle animation loop (breathing, looking around using sprite frames)
    - Has physics body for collision with player
    - Implements facePlayer(playerX, playerY) method to flip sprite direction
    - Stores dialogueId for later dialogue system hookup
  </action>
  <verify>NPC sprite renders at spawn position with looping idle animation and blocks player movement</verify>
  <done>NPC.ts class exists with idle animation, collision, and player-facing logic</done>
</task>

<task type="auto">
  <name>Task 3: Add interaction prompt display in VillageScene</name>
  <files>frontend/src/app/game/scenes/VillageScene.ts</files>
  <action>
    Modify VillageScene.ts to:
    - Import and instantiate NPCManager in create()
    - In update(), call NPCManager.getClosestNPC(player.x, player.y, 32)
    - If NPC is nearby, display "Press SPACE to talk" text above NPC or as UI overlay
    - Hide prompt when player moves away (distance > 32 pixels)
    - Store current interactable NPC reference for next phase dialogue hookup
    - Add SPACE key listener (handler stubbed for now, just console.log)
  </action>
  <verify>Walking near Claude NPC shows interaction prompt, walking away hides it</verify>
  <done>VillageScene shows/hides prompt based on proximity, SPACE key logs to console</done>
</task>
</tasks>

<verification>
1. Run game and navigate to town square
2. Verify Claude NPC sprite is visible with idle animation
3. Walk within 32 pixels of NPC - interaction prompt appears
4. Walk away - prompt disappears
5. Pressing SPACE near NPC logs to console
</verification>

<success_criteria>
- NPC-01: Claude NPC stands in town square with idle animation ✓
- NPC-02: Walking near an NPC shows interaction prompt ("Press SPACE to talk") ✓
</success_criteria>

<output>Create `.planning/phases/03-npc-and-dialogue/03-01-SUMMARY.md`</output>
