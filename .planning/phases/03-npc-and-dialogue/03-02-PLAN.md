---
phase: 03-npc-and-dialogue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/ui/DialogueBox.ts
  - frontend/src/app/game/scenes/VillageScene.ts
autonomous: true
must_haves:
  truths:
    - NPC-03: RPG dialogue box at bottom of screen with typewriter text effect
    - NPC-06: Space/Enter advances text or instant-completes current line
    - NPC-07: Player movement paused during active dialogue
  artifacts:
    - DialogueBox.ts UI component with typewriter animation
    - Integration in VillageScene that pauses player input
    - Working SPACE/ENTER advance and instant-complete logic
  key_links:
    - "@.planning/ROADMAP.md Phase 3 success criteria"
---

<objective>Build RPG-style dialogue box with typewriter effect and input controls
Purpose: Visitors experience classic RPG text presentation when talking to NPCs
Output: DialogueBox.ts component and VillageScene integration with text animation and input handling</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create DialogueBox UI component with typewriter effect</name>
  <files>frontend/src/app/game/ui/DialogueBox.ts</files>
  <action>
    Create DialogueBox.ts class that:
    - Extends Phaser.GameObjects.Container
    - Renders a panel at bottom 20% of screen with RPG-style border
    - Displays NPC name and portrait (portrait can be optional for v1)
    - Implements typewriter text reveal (configurable speed, default 30ms per char)
    - Shows "▼ Press ENTER to continue" indicator when text is complete
    - Provides show(text, npcName, onComplete) and hide() methods
    - Emits 'textComplete' and 'dialogueAdvance' events
    - Handles SPACE/ENTER key: if text is animating → instant-complete; if complete → advance
  </action>
  <verify>DialogueBox renders at screen bottom with text appearing character-by-character</verify>
  <done>DialogueBox.ts exists with working typewriter animation and advance/complete controls</done>
</task>

<task type="auto">
  <name>Task 2: Integrate DialogueBox into VillageScene with player pause</name>
  <files>frontend/src/app/game/scenes/VillageScene.ts</files>
  <action>
    Modify VillageScene.ts to:
    - Import and instantiate DialogueBox in create()
    - Add dialogueActive boolean flag (default false)
    - When SPACE pressed near NPC: set dialogueActive=true, call dialogueBox.show() with test text
    - In player update() loop: skip movement input processing if dialogueActive is true
    - Listen for dialogueBox 'dialogueComplete' event to set dialogueActive=false and hide box
    - Add ESC key handler to close dialogue early (optional but nice UX)
  </action>
  <verify>Player cannot move during dialogue, WASD/arrow keys have no effect until dialogue ends</verify>
  <done>VillageScene pauses player input during dialogue and resumes when dialogue closes</done>
</task>

<task type="auto">
  <name>Task 3: Add multi-page dialogue support</name>
  <files>frontend/src/app/game/ui/DialogueBox.ts</files>
  <action>
    Extend DialogueBox.ts to:
    - Accept array of text pages in show() method
    - Track current page index
    - On ENTER/SPACE after text complete: advance to next page
    - On final page advance: emit 'dialogueComplete' event
    - Show page indicator (e.g., "1/3") if multiple pages exist
    - Clear previous text before showing next page
  </action>
  <verify>DialogueBox can display 3-page test dialogue, advancing through pages with ENTER</verify>
  <done>DialogueBox supports multi-page dialogue with page tracking and completion event</done>
</task>
</tasks>

<verification>
1. Run game and walk to Claude NPC
2. Press SPACE to open dialogue
3. Verify text appears character-by-character with typewriter effect
4. Try moving with WASD - verify player does NOT move
5. Press SPACE during animation - text instant-completes
6. Press SPACE again - advances to next page
7. After final page, dialogue closes and player can move again
</verification>

<success_criteria>
- NPC-03: RPG dialogue box at bottom of screen with typewriter text effect ✓
- NPC-06: Space/Enter advances text or instant-completes current line ✓
- NPC-07: Player movement paused during active dialogue ✓
</success_criteria>

<output>Create `.planning/phases/03-npc-and-dialogue/03-02-SUMMARY.md`</output>
