---
phase: 03-npc-and-dialogue
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - frontend/src/app/game/dialogue/DialogueTree.ts
  - frontend/src/app/game/ui/DialogueBox.ts
  - frontend/src/app/game/scenes/VillageScene.ts
autonomous: true
must_haves:
  truths:
    - NPC-04: Dialogue supports branching choices
    - NPC-05: Dialogue JSON tree format with per-NPC dialogue files
  artifacts:
    - DialogueTree.ts parser for dialogue node structure
    - Modified DialogueBox.ts with choice button rendering
    - Working branching conversation flow
  key_links:
    - "@.planning/ROADMAP.md Phase 3 success criteria"
---

<objective>Build branching dialogue tree engine with choice rendering
Purpose: Visitors make meaningful dialogue choices that lead to different conversation paths
Output: DialogueTree.ts parser and extended DialogueBox with working choice buttons and conditional flow</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create DialogueTree data structure and parser</name>
  <files>frontend/src/app/game/dialogue/DialogueTree.ts</files>
  <action>
    Create DialogueTree.ts with:
    - Interface DialogueNode { id: string, speaker: string, text: string, choices?: Choice[], nextNodeId?: string, condition?: string, triggers?: Trigger[] }
    - Interface Choice { text: string, nextNodeId: string, condition?: string }
    - Interface Trigger { type: 'startQuest' | 'giveItem' | 'setFlag', data: any }
    - Class DialogueTree with constructor(treeData: DialogueNode[])
    - Method getNode(nodeId: string): DialogueNode
    - Method evaluateCondition(condition: string, gameState: any): boolean
    - Method getAvailableChoices(node: DialogueNode, gameState: any): Choice[]
    - Parse dialogue tree from JSON loaded via Phaser cache
  </action>
  <verify>DialogueTree can parse JSON structure and return nodes with filtered choices</verify>
  <done>DialogueTree.ts exists with complete node traversal and condition evaluation</done>
</task>

<task type="auto">
  <name>Task 2: Add choice rendering to DialogueBox</name>
  <files>frontend/src/app/game/ui/DialogueBox.ts</files>
  <action>
    Extend DialogueBox.ts to:
    - Accept optional choices array in show() method
    - When choices present: render 2-4 choice buttons below text area
    - Style choice buttons as RPG menu items (hover highlight, number indicators)
    - Support keyboard selection (1-4 keys OR arrow keys + ENTER)
    - Emit 'choiceSelected' event with choice object
    - Hide choice buttons when showing next dialogue node
    - Maintain typewriter effect for choice node text before showing buttons
  </action>
  <verify>DialogueBox can render up to 4 choice buttons and emit selection event</verify>
  <done>DialogueBox supports choice rendering with keyboard selection and event emission</done>
</task>

<task type="auto">
  <name>Task 3: Integrate DialogueTree into VillageScene conversation flow</name>
  <files>frontend/src/app/game/scenes/VillageScene.ts</files>
  <action>
    Modify VillageScene.ts to:
    - Load test dialogue JSON in preload() for Claude NPC
    - Create DialogueTree instance from loaded data
    - In startDialogue(): get root node from tree and pass to dialogueBox.show()
    - Listen for 'choiceSelected' event: get next node and continue conversation
    - Listen for 'dialogueAdvance': if no choices, get nextNodeId and show next node
    - Handle dialogue triggers when encountered (stub handlers for now)
    - End dialogue when node has no nextNodeId and no choices
    - Maintain gameState object for condition evaluation (can start empty)
  </action>
  <verify>Full branching conversation works: choice A leads to path A, choice B leads to path B</verify>
  <done>VillageScene implements complete dialogue tree traversal with working branches</done>
</task>
</tasks>

<verification>
1. Run game and start conversation with Claude NPC
2. Verify dialogue presents 2+ choices
3. Select choice 1 - verify conversation follows branch 1
4. Restart conversation, select choice 2 - verify different dialogue path
5. Complete conversation to end node - dialogue closes
6. Re-test with conditional choice (if implemented in test JSON)
</verification>

<success_criteria>
- NPC-04: Dialogue supports branching choices ✓
- NPC-05: Dialogue JSON tree format with per-NPC dialogue files ✓
</success_criteria>

<output>Create `.planning/phases/03-npc-and-dialogue/03-03-SUMMARY.md`</output>
