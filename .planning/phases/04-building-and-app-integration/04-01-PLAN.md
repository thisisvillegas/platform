---
phase: 04-building-and-app-integration
plan: 01
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - frontend/src/app/game/systems/BuildingManager.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/app/game/services/world-navigation.service.ts
  - backend/src/routes/guestRoutes.ts
  - backend/src/models/GuestAccount.ts
autonomous: true
must_haves:
  truths:
    - Walking to a building door shows "Press ENTER to enter" prompt
    - Pressing ENTER navigates to the app's route or external URL
    - Auth-required buildings trigger receptionist NPC dialogue first
    - Guest accounts are auto-created via POST /api/guest/create
  artifacts:
    - BuildingManager.ts: Parses door zones from Tiled object layer, detects player proximity
    - world-navigation.service.ts: Handles navigation to apps with state preservation
    - guestRoutes.ts: POST /api/guest/create endpoint for auto-provisioning
  key_links:
    - Door zones defined in Tiled map as object layer "doors" with buildingId property
    - Building metadata in world pack manifest.json specifies appUrl and requiresAuth
---

<objective>
Implement building entry system that detects door zones, shows interaction prompts, and navigates to live apps (with lobby flow for auth-required buildings).
Purpose: Enable core portfolio functionality -- walking into a building in the game world takes you to the actual running app.
Output: Working BuildingManager with door detection, navigation service, and guest account creation for auth-required apps.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BuildingManager system with door zone detection</name>
  <files>frontend/src/app/game/systems/BuildingManager.ts</files>
  <action>
  Create BuildingManager.ts that:
  - Loads door zones from Tiled object layer named "doors"
  - Each door object has properties: buildingId, x, y, width, height
  - Detects player entering door zone using rectangle collision
  - Shows interaction prompt UI: "Press ENTER to enter [Building Name]"
  - Hides prompt when player exits door zone
  - Reads building metadata from world pack manifest.json
  - Returns building config (appUrl, requiresAuth, name) when door activated
  - Integrates with OverworldScene to handle ENTER key input
  </action>
  <verify>
  - Walk to building door, prompt appears
  - Walk away, prompt disappears
  - Press ENTER at door, navigation is triggered
  </verify>
  <done>
  BuildingManager detects door zones and shows/hides prompts based on player position
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement navigation service for app routing</name>
  <files>frontend/src/app/game/services/world-navigation.service.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
  Create world-navigation.service.ts that:
  - Saves current player position to sessionStorage before navigation
  - Saves current building ID being entered
  - Saves game scene state (pause, don't destroy)
  - Navigates to app URL via Angular Router
  - Handles both same-domain routes (/brain-dump) and external URLs (https://homecontrol.thisisvillegas.com)
  - Passes guest credentials via URL params or localStorage
  - For external apps: window.location.href navigation
  - For internal apps: Angular Router.navigate()

  Integrate with OverworldScene:
  - On ENTER key at door, check building.requiresAuth
  - If requiresAuth is false: immediately call navigation service
  - If requiresAuth is true: trigger lobby flow (next task)
  </action>
  <verify>
  - Enter non-auth building, navigates to app route
  - Check sessionStorage contains savedPosition and currentBuildingId
  - Game scene is paused, not destroyed
  </verify>
  <done>
  Navigation service saves state and routes to app URLs correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Add lobby scene with receptionist NPC for auth-required buildings</name>
  <files>frontend/src/app/game/scenes/LobbyScene.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
  Create LobbyScene.ts that:
  - Loads interior tilemap for building (from world pack: buildings/[buildingId]/interior.json)
  - Spawns receptionist NPC at configured position
  - Triggers dialogue tree: "Welcome! I'll create your guest account..."
  - Calls POST /api/guest/create with buildingId on dialogue completion
  - Shows "You're all set!" confirmation message
  - Inner door zone becomes active after account creation
  - Entering inner door navigates to app URL

  Update OverworldScene:
  - If building.requiresAuth is true, transition to LobbyScene instead of direct navigation
  - Pass building config to LobbyScene via scene data
  </action>
  <verify>
  - Enter auth-required building, loads lobby interior
  - Receptionist NPC appears with dialogue
  - After dialogue, API call creates guest account
  - Inner door opens and navigates to app
  </verify>
  <done>
  Auth-required buildings show lobby with receptionist that creates guest accounts before app entry
  </done>
</task>

<task type="auto">
  <name>Task 4: Create guest account API endpoint</name>
  <files>backend/src/routes/guestRoutes.ts, backend/src/models/GuestAccount.ts</files>
  <action>
  Create GuestAccount.ts model:
  - Schema: guestId (UUID), passId (reference), appId (string), credentials (object), createdAt, expiresAt
  - Methods: generateCredentials(), isExpired()

  Create guestRoutes.ts:
  - POST /api/guest/create
  - Body: { passId, appId, buildingId }
  - Validate passId exists and is not expired
  - Generate app-specific guest credentials (username, temp password)
  - Store in GuestAccount collection
  - Return: { success, credentials: { username, password, appUrl } }
  - Error handling: expired pass, invalid appId, duplicate account
  </action>
  <verify>
  - POST /api/guest/create with valid passId returns guest credentials
  - Guest account appears in MongoDB collection
  - Expired pass returns error
  </verify>
  <done>
  API endpoint creates and returns guest account credentials for auth-required apps
  </done>
</task>

</tasks>

<verification>
1. Walk to building door without auth requirement - "Press ENTER to enter" appears
2. Press ENTER - navigates directly to app route
3. Check sessionStorage - contains savedPosition and currentBuildingId
4. Walk to auth-required building door - loads lobby interior scene
5. Talk to receptionist NPC - dialogue completes and calls /api/guest/create
6. Enter inner door - navigates to app with guest credentials
7. Check MongoDB - guest account exists with credentials
</verification>

<success_criteria>
1. Door zones detect player proximity and show interaction prompts (BLDG-01)
2. Entering buildings navigates to app routes or external URLs (BLDG-02)
3. Auth-required buildings show lobby with receptionist before app entry
4. Guest accounts are auto-created via API for auth-required apps
5. Player position and building ID saved to sessionStorage before navigation (partial BLDG-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-building-and-app-integration/04-01-SUMMARY.md`
</output>
