---
phase: 04-building-and-app-integration
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - frontend/src/app/components/return-to-world-button/return-to-world-button.component.ts
  - frontend/src/app/components/return-to-world-button/return-to-world-button.component.html
  - frontend/src/app/components/return-to-world-button/return-to-world-button.component.scss
  - frontend/src/app/app.component.ts
  - frontend/src/app/game/services/world-navigation.service.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - backend/src/routes/visitorProgressRoutes.ts
  - backend/src/models/VisitorProgress.ts
autonomous: true
must_haves:
  truths:
    - Floating "Return to World" button visible on all app pages except /world, /dashboard, /door
    - Clicking button restores player to position outside building they entered
    - Player position persists in sessionStorage for same session
    - Player position saved to backend for cross-session persistence
    - Game scene resumes exactly as left (paused, not destroyed)
  artifacts:
    - return-to-world-button.component.ts: Persistent floating button with pixel-art styling
    - world-navigation.service.ts: restoreFromApp() method to handle return flow
    - visitorProgressRoutes.ts: PUT /api/progress/position endpoint for persistence
  key_links:
    - Button shows only for pass-holders (checks guest JWT in localStorage)
    - Position saved as { x, y, lastBuildingId, sceneKey }
---

<objective>
Implement "Return to World" button that appears on all app pages and restores player position when returning from building visits.
Purpose: Enable seamless navigation loop -- enter building, use app, return to exact position in game world.
Output: Working return button with position restoration from both sessionStorage (same session) and backend (cross-session).
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Return to World button component</name>
  <files>frontend/src/app/components/return-to-world-button/return-to-world-button.component.ts, frontend/src/app/components/return-to-world-button/return-to-world-button.component.html, frontend/src/app/components/return-to-world-button/return-to-world-button.component.scss</files>
  <action>
  Create standalone Angular component return-to-world-button:

  TypeScript:
  - Check localStorage for guest JWT token
  - Only show button if token exists and role is "guest"
  - Hide on routes: /world, /dashboard, /door, auth pages
  - Click handler: call worldNavigationService.restoreFromApp()
  - Position: fixed, bottom-right corner
  - Z-index: 9999 (above all app content)

  HTML:
  - Pixel-art styled button with "◄ Return to World" text
  - Hover state with slight scale animation
  - Click triggers navigation back to /world

  SCSS:
  - Sci-fi pixel aesthetic matching game world
  - Fixed positioning: bottom: 20px, right: 20px
  - Background: translucent dark with pixel border
  - Font: monospace, pixel-style
  - Animation: subtle pulse on hover
  - Mobile responsive: smaller size, bottom-left on small screens
  </action>
  <verify>
  - Button appears on /brain-dump, /rootine, etc. for guest users
  - Button does NOT appear on /world, /dashboard, /door
  - Button hidden for non-guest users
  - Click navigates to /world
  </verify>
  <done>
  Return to World button component renders on app pages and navigates back to game world
  </done>
</task>

<task type="auto">
  <name>Task 2: Add button to app root component</name>
  <files>frontend/src/app/app.component.ts, frontend/src/app/app.component.html</files>
  <action>
  Update app.component.html:
  - Add <app-return-to-world-button /> before router-outlet
  - Import ReturnToWorldButtonComponent in app.component.ts
  - Component self-manages visibility based on route and auth state

  Ensure button persists across all routes:
  - Placed at root level, not inside lazy-loaded modules
  - Always in DOM, but conditionally visible
  </action>
  <verify>
  - Navigate through different app routes - button persists
  - Go to /world - button disappears
  - Return to app route - button reappears
  </verify>
  <done>
  Button is globally available across all app pages via root component
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement position restoration in navigation service</name>
  <files>frontend/src/app/game/services/world-navigation.service.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
  Add to world-navigation.service.ts:

  restoreFromApp() method:
  - Navigate to /world route
  - Read savedPosition from sessionStorage
  - If found: emit event to OverworldScene with { x, y, lastBuildingId }
  - If not found: fallback to backend GET /api/progress/position
  - Resume game scene (unpause)
  - Clear loading state

  Update savePlayerPosition() (from 04-01):
  - Save to sessionStorage: { x, y, lastBuildingId, timestamp }
  - Also save to backend: PUT /api/progress/position
  - Debounce backend saves (only every 5 seconds)

  Update OverworldScene.ts:
  - Listen for positionRestore event from service
  - On restore: move player sprite to saved x, y
  - Position player outside building entrance (offset from door zone)
  - Resume camera tracking
  - Unpause scene
  </action>
  <verify>
  - Enter building, return via button - player spawns outside correct building
  - Close browser, reopen, return - player position restored from backend
  - sessionStorage has savedPosition object
  - Backend PUT /api/progress/position called on position save
  </verify>
  <done>
  Position restoration works from sessionStorage (same session) and backend (cross-session)
  </done>
</task>

<task type="auto">
  <name>Task 4: Create visitor progress API endpoints</name>
  <files>backend/src/routes/visitorProgressRoutes.ts, backend/src/models/VisitorProgress.ts</files>
  <action>
  Create VisitorProgress.ts model:
  - Schema: passId (reference), position { x, y, lastBuildingId, sceneKey }, updatedAt
  - Index on passId for fast lookups
  - Methods: updatePosition(), getPosition()

  Create visitorProgressRoutes.ts:
  - PUT /api/progress/position
    - Body: { passId, x, y, lastBuildingId, sceneKey }
    - Upsert visitor progress document
    - Return success status

  - GET /api/progress/position/:passId
    - Return saved position or default spawn point
    - Include lastBuildingId for restoration context

  Authentication:
  - Require valid guest JWT or owner Auth0 token
  - Validate passId matches JWT claim
  </action>
  <verify>
  - PUT /api/progress/position with guest JWT saves position to MongoDB
  - GET /api/progress/position/:passId returns saved position
  - Invalid JWT returns 401
  - Position data structure matches expected format
  </verify>
  <done>
  API endpoints for saving and retrieving player position across sessions are working
  </done>
</task>

<task type="auto">
  <name>Task 5: Handle page unload position save</name>
  <files>frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
  Add beforeunload listener to OverworldScene:
  - On window beforeunload event
  - Save current player position to sessionStorage
  - Quick synchronous save (no backend call - too slow)
  - Backend save handled by periodic debounced updates

  Cleanup:
  - Remove listener on scene shutdown
  - Prevent memory leaks from multiple listeners
  </action>
  <verify>
  - Refresh page while in world - position saved to sessionStorage
  - Return from app after refresh - position restored
  </verify>
  <done>
  Player position persists across page refreshes via sessionStorage unload save
  </done>
</task>

</tasks>

<verification>
1. Navigate to app page as guest - "Return to World" button appears bottom-right
2. Click button - returns to /world route
3. Player spawns outside the building they entered (not at world spawn)
4. Enter building again, refresh app page, click return - position still restored
5. Close browser, reopen app, click return - position restored from backend
6. Check sessionStorage - contains savedPosition object
7. Check MongoDB - visitorProgress document exists with position data
8. Visit /dashboard - button does not appear
</verification>

<success_criteria>
1. Floating "Return to World" button visible on all app pages (BLDG-03)
2. Returning to world respawns player outside building they entered (BLDG-04)
3. Player position saved in sessionStorage for same-session persistence (BLDG-05)
4. Player position saved to backend for cross-session persistence (BLDG-05)
5. Game pauses when navigating to apps, resumes on return (BLDG-06)
</success_criteria>

<output>
After completion, create `.planning/phases/04-building-and-app-integration/04-02-SUMMARY.md`
</output>
