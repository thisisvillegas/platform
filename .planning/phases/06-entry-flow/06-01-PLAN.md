---
phase: 06-entry-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/scenes/CinematicScene.ts
  - frontend/src/app/game/PhaserGameComponent.ts
  - frontend/src/app/app.routes.ts
autonomous: true
must_haves:
  truths:
    - ENTRY-01: Visiting thisisvillegas.com shows cinematic intro with typewriter text and particles
    - ENTRY-02: Pulsing "Enter" button transitions to door scene on click
  artifacts:
    - CinematicScene.ts Phaser scene with fade-in, particles, typewriter effect
    - Angular route at / that loads CinematicScene
    - Enter button sprite with pulse animation
  key_links:
    - "@.planning/ROADMAP.md Phase 6 success criteria"
---

<objective>Build the cinematic intro scene that plays when visitors first arrive at thisisvillegas.com
Purpose: Create an impressive first impression with typewriter text, floating particles, and smooth transition to door
Output: CinematicScene.ts with ambient visuals, text animation, and Enter button that transitions to door scene</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create CinematicScene with dark fade-in and ambient particles</name>
  <files>frontend/src/app/game/scenes/CinematicScene.ts</files>
  <action>
    Create CinematicScene.ts that:
    - Extends Phaser.Scene with key 'CinematicScene'
    - Starts with black screen that fades in over 2 seconds
    - Creates particle emitter for floating glowing dots (white/cyan particles drifting upward)
    - Optional: code snippet particles (small text sprites like "const", "=>", "{}" floating by)
    - Sets ambient background (dark gradient or solid dark blue/black)
    - Handles window resize to keep particles centered
  </action>
  <verify>Scene loads with smooth fade-in from black, particles drift across screen continuously</verify>
  <done>CinematicScene.ts exists with fade-in transition and floating particle effects</done>
</task>

<task type="auto">
  <name>Task 2: Add typewriter text animation</name>
  <files>frontend/src/app/game/scenes/CinematicScene.ts</files>
  <action>
    Modify CinematicScene.ts to:
    - Add Phaser.GameObjects.Text centered on screen
    - Implement typewriter effect for: "Built by a human. Powered by Claude. Enter the world."
    - Use monospace or pixel font if available, otherwise default system font
    - Character reveal rate: 50ms per character
    - After text completes, hold for 1 second before showing Enter button
    - Text should be white or cyan with slight glow/shadow for readability
  </action>
  <verify>Text appears character-by-character after fade-in completes, full sentence visible before button appears</verify>
  <done>Typewriter animation plays with proper timing, text is readable over particle effects</done>
</task>

<task type="auto">
  <name>Task 3: Add pulsing Enter button with transition to DoorScene</name>
  <files>frontend/src/app/game/scenes/CinematicScene.ts</files>
  <action>
    Modify CinematicScene.ts to:
    - Create Enter button (rounded rectangle or sprite) centered at bottom of screen
    - Add text label "ENTER" or "⏎ ENTER"
    - Implement pulse animation (scale tween 1.0 to 1.1 looping, or alpha fade)
    - Add click/tap handler that fades out scene and transitions to DoorScene
    - On click: fade to black over 1 second, then call this.scene.start('DoorScene')
    - Add pointer hover effect (scale up or color change on hover)
  </action>
  <verify>Enter button pulses continuously, clicking triggers fade-out and scene transition</verify>
  <done>Enter button is interactive with pulse animation, transitions to DoorScene on click</done>
</task>

<task type="auto">
  <name>Task 4: Wire CinematicScene into Angular routing and PhaserGameComponent</name>
  <files>
    frontend/src/app/game/PhaserGameComponent.ts
    frontend/src/app/app.routes.ts
  </files>
  <action>
    Modify PhaserGameComponent.ts to:
    - Import CinematicScene
    - Add CinematicScene to Phaser config scene array
    - If route param or default is "cinematic", set scene to CinematicScene
    - Handle scene transitions (CinematicScene -> DoorScene)

    Modify app.routes.ts to:
    - Update root route "/" to load game component with scene: 'cinematic'
    - Ensure lazy-loaded Phaser module loads for root route
    - Keep existing /world, /door routes functional
  </action>
  <verify>Navigating to thisisvillegas.com loads PhaserGameComponent with CinematicScene as initial scene</verify>
  <done>Root route shows cinematic intro, scene transitions work, no route conflicts</done>
</task>
</tasks>

<verification>
1. Navigate to http://localhost:4200/ (or thisisvillegas.com on Pi)
2. Verify black screen fades in to show particles drifting
3. Verify typewriter text appears character-by-character
4. Verify "Enter" button appears after text completes, with pulse animation
5. Click Enter button - verify fade to black and transition to DoorScene (even if DoorScene is empty for now)
6. No console errors, scene runs at 60fps
</verification>

<success_criteria>
- ENTRY-01: Visiting thisisvillegas.com shows cinematic intro with typewriter text and particles ✓
- ENTRY-02: Pulsing "Enter" button transitions to door scene on click ✓
</success_criteria>

<output>Create `.planning/phases/06-entry-flow/06-01-SUMMARY.md`</output>
