---
phase: 06-entry-flow
plan: 02
type: execute
wave: 1
depends_on: ["05-01", "05-02"]
files_modified:
  - frontend/src/app/game/scenes/DoorScene.ts
  - frontend/src/app/game/PhaserGameComponent.ts
  - frontend/src/app/services/pass-validation.service.ts
autonomous: true
must_haves:
  truths:
    - ENTRY-03: Door scene shows speakeasy keypad for code entry
    - ENTRY-04: Wrong code triggers door rattle animation with shake and denial sound
    - ENTRY-05: Correct code triggers door swing animation and transitions to game world
  artifacts:
    - DoorScene.ts Phaser scene with first-person door view and keypad UI
    - PassValidationService that calls POST /api/passes/validate
    - Door swing animation sequence (Resident Evil-style camera shift)
  key_links:
    - "@.planning/ROADMAP.md Phase 6 success criteria"
    - "@.planning/phases/05-pass-and-guest-auth/ for pass validation API"
---

<objective>Build the speakeasy door scene with code entry, validation, and Resident Evil-style door animation
Purpose: Visitors enter their access code and experience cinematic feedback for success or failure
Output: DoorScene.ts with keypad UI, pass validation integration, door rattle/swing animations</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pass-and-guest-auth/
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create DoorScene with first-person door view and keypad UI</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Create DoorScene.ts that:
    - Extends Phaser.Scene with key 'DoorScene'
    - Renders first-person view of a heavy speakeasy door (sprite or drawn rectangle with wood texture)
    - Adds speakeasy-style keypad UI: input field for code entry, styled as metal slot or digital keypad
    - Supports keyboard input (auto-focus on scene start) and touch input (mobile)
    - Visual feedback for each keypress (highlight, click sound, character mask like "****")
    - Display character counter or code length indicator (e.g., "0/8")
    - Add Submit button or auto-submit on N characters (match expected pass code length)
    - Rate limit: 3 attempts per 30 seconds client-side, show cooldown timer if limit exceeded
  </action>
  <verify>Door scene loads with door graphic and keypad, typing shows masked characters, submit is accessible</verify>
  <done>DoorScene.ts renders door view with functional keypad UI and rate limiting</done>
</task>

<task type="auto">
  <name>Task 2: Create PassValidationService for API integration</name>
  <files>frontend/src/app/services/pass-validation.service.ts</files>
  <action>
    Create PassValidationService that:
    - Injectable service in Angular
    - Method validateCode(code: string): Observable<{ valid: boolean, token?: string, error?: string }>
    - Calls POST /api/passes/validate with { code } in request body
    - Returns success: { valid: true, token: "jwt..." }
    - Returns failure: { valid: false, error: "Invalid code" | "Code expired" | "Rate limited" }
    - Stores guest JWT in localStorage on success (key: 'guest_token')
    - Handles HTTP errors (network failure, server down)
  </action>
  <verify>Service can call pass validation API and return structured response</verify>
  <done>PassValidationService.ts exists with validateCode method and JWT storage</done>
</task>

<task type="auto">
  <name>Task 3: Integrate pass validation with door rattle animation on failure</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Modify DoorScene.ts to:
    - Import PassValidationService (via Angular-Phaser bridge or event bus)
    - On code submit, call PassValidationService.validateCode(enteredCode)
    - On validation failure:
      * Play door rattle animation (shake door sprite left-right 5-10px for 0.5 seconds)
      * Play denial sound effect (low buzz, clunk, or lock sound)
      * Show error message below keypad: "Invalid code" or "Code expired"
      * Clear input field after 2 seconds
      * Increment attempt counter for rate limiting
    - On rate limit exceeded: show "Too many attempts. Try again in Xs" countdown
  </action>
  <verify>Entering wrong code triggers door shake, denial sound, and error message display</verify>
  <done>Door rattle animation and failure feedback work correctly with pass validation</done>
</task>

<task type="auto">
  <name>Task 4: Implement door swing animation and transition to VillageScene on success</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Modify DoorScene.ts to:
    - On validation success (valid: true, token received):
      * Store JWT in localStorage (PassValidationService handles this)
      * Play door opening sound effect (heavy door creak, lock click)
      * Animate Resident Evil-style door sequence:
        - Camera angle shift (rotate/scale door sprite to simulate perspective change)
        - Door swings open (rotate door sprite from 0° to 90° over 1.5 seconds)
        - Fade to black during/after door swing (1 second fade)
      * Call this.scene.start('VillageScene') to transition to game world
      * Optional: show "Welcome" or loading text during fade
  </action>
  <verify>Entering correct code triggers door swing animation, sound, fade to black, and loads VillageScene</verify>
  <done>Door swing animation sequence completes and transitions to game world on valid code</done>
</task>

<task type="auto">
  <name>Task 5: Wire DoorScene into PhaserGameComponent scene config</name>
  <files>frontend/src/app/game/PhaserGameComponent.ts</files>
  <action>
    Modify PhaserGameComponent.ts to:
    - Import DoorScene
    - Add DoorScene to Phaser config scene array
    - Handle route param for /door to set initial scene to DoorScene
    - Ensure Angular-Phaser bridge allows DoorScene to access PassValidationService
      (inject service into component, expose via EventBus or shared registry)
  </action>
  <verify>Navigating to /door loads DoorScene, CinematicScene can transition to DoorScene</verify>
  <done>DoorScene is registered and accessible via routing and scene transitions</done>
</task>
</tasks>

<verification>
1. Navigate to /door (or click Enter on cinematic intro)
2. Verify door graphic renders with keypad UI
3. Type an invalid code and submit - verify door shake, denial sound, error message
4. Wait for rate limit cooldown if triggered
5. Type a valid code (from Phase 5 pass system) and submit
6. Verify door swing animation plays with opening sound
7. Verify fade to black and transition to VillageScene
8. Check localStorage for guest_token
9. No console errors, animations run smoothly
</verification>

<success_criteria>
- ENTRY-03: Door scene shows speakeasy keypad for code entry ✓
- ENTRY-04: Wrong code triggers door rattle animation with shake and denial sound ✓
- ENTRY-05: Correct code triggers door swing animation and transitions to game world ✓
</success_criteria>

<output>Create `.planning/phases/06-entry-flow/06-02-SUMMARY.md`</output>
