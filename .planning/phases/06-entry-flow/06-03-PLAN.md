---
phase: 06-entry-flow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/scenes/DoorScene.ts
  - frontend/src/app/components/no-code-message/no-code-message.component.ts
  - frontend/src/app/app.routes.ts
autonomous: true
must_haves:
  truths:
    - ENTRY-06: No-code visitors see clear message about requesting access with contact/social links
    - ENTRY-07: Skip link is visible for direct project list access
  artifacts:
    - No-code message overlay or separate component with contact links
    - Skip link that navigates to /projects or app list
    - Help/info button in DoorScene for no-code visitors
  key_links:
    - "@.planning/ROADMAP.md Phase 6 success criteria"
---

<objective>Provide a welcoming experience for visitors without an access code and add skip option for direct access
Purpose: No-code visitors understand how to request access and can opt out of the game experience
Output: No-code message UI with contact links, Skip link in DoorScene for direct project navigation</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add "Need a code?" button to DoorScene</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Modify DoorScene.ts to:
    - Add small text button or link below keypad: "Need a code?" or "Don't have access?"
    - On click, show modal/overlay within Phaser scene OR emit event to Angular to show component
    - Modal content:
      * Title: "This experience is invite-only"
      * Description: "This portfolio world is currently private. Request an access code by reaching out:"
      * Contact/social links (email, LinkedIn, GitHub, Twitter/X)
      * Button: "Close" to dismiss modal
    - Style modal to match speakeasy/sci-fi aesthetic (dark background, cyan borders, pixel font)
  </action>
  <verify>Clicking "Need a code?" shows modal with request access message and contact links</verify>
  <done>DoorScene has "Need a code?" button that displays contact info modal</done>
</task>

<task type="auto">
  <name>Task 2: Add Skip link for direct project list access</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Modify DoorScene.ts to:
    - Add text link in top-right corner or bottom of screen: "Skip to projects →"
    - On click, navigate to /projects route (Angular navigation)
    - Emit event from Phaser to Angular to trigger router.navigate(['/projects'])
    - Style as subtle link (not competing with main door interaction)
    - Optional: show confirmation dialog "Skip the game and view projects directly?"
  </action>
  <verify>Clicking Skip link navigates to /projects route, bypassing door/world experience</verify>
  <done>DoorScene has functional Skip link that navigates to project list</done>
</task>

<task type="auto">
  <name>Task 3: Create /projects route with simple project list (if not exists)</name>
  <files>
    frontend/src/app/app.routes.ts
    frontend/src/app/components/project-list/project-list.component.ts
  </files>
  <action>
    Check if /projects route exists:
    - If exists: verify it loads a project list component
    - If not exists:
      * Create ProjectListComponent with standalone component
      * List all portfolio apps with links (Brain-Dump, Homecontrol, Rootine, TactIQal, GIF Gallery)
      * Include brief description for each app
      * Style with sci-fi aesthetic matching rest of site
      * Add route to app.routes.ts: { path: 'projects', component: ProjectListComponent }
    - Ensure no authentication required for /projects (public route)
  </action>
  <verify>Navigating to /projects shows list of apps with clickable links</verify>
  <done>/projects route exists and displays project list for visitors who skip game</done>
</task>

<task type="auto">
  <name>Task 4: Optional - Add "peek through keyhole" teaser for no-code visitors</name>
  <files>frontend/src/app/game/scenes/DoorScene.ts</files>
  <action>
    Optional enhancement to DoorScene:
    - Add small circular "keyhole" graphic in door sprite
    - On hover or click on keyhole, show blurred preview of game world (screenshot or live render with blur filter)
    - Display text: "A glimpse of what awaits inside..."
    - After 3 seconds, blur back to door view
    - This teases the game experience without giving full access
    - Only implement if time permits, not blocking for phase completion
  </action>
  <verify>Keyhole preview shows blurred world, entices visitors to request code</verify>
  <done>Keyhole teaser implemented (optional) or skipped if time-constrained</done>
</task>
</tasks>

<verification>
1. Navigate to /door without a valid code
2. Click "Need a code?" button - verify modal shows with contact info
3. Click contact links - verify they open correctly (email, social profiles)
4. Close modal and verify return to door scene
5. Click "Skip to projects" link - verify navigation to /projects route
6. Verify /projects shows list of portfolio apps
7. Optional: Hover/click keyhole to see blurred world preview
8. All interactions smooth, no console errors
</verification>

<success_criteria>
- ENTRY-06: No-code visitors see clear message about requesting access with contact/social links ✓
- ENTRY-07: Skip link is visible for direct project list access ✓
</success_criteria>

<output>Create `.planning/phases/06-entry-flow/06-03-SUMMARY.md`</output>
