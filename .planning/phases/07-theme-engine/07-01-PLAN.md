---
phase: 07-theme-engine
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 02-game-world
    provides: Phaser scene system, tilemap rendering, camera controls
files_modified:
  - frontend/src/app/game/systems/ThemeEngine.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/app/game/models/Theme.ts
  - frontend/src/assets/worlds/village/themes/default.json
  - frontend/src/assets/worlds/village/themes/night.json
autonomous: true
must_haves:
  truths:
    - Theme JSON is loaded from world pack themes/ directory at scene start
    - Camera applies color tint pipeline for night mode (dark blue overlay)
    - Lamp objects emit light cones at night using Phaser light system
    - Window tiles swap to lit variants at night
    - Day/night auto-detects from visitor's local time
    - Toggle button exists in game UI to override auto-detection
  artifacts:
    - frontend/src/app/game/systems/ThemeEngine.ts: Core theme system with JSON loading, palette application, and runtime switching
    - frontend/src/app/game/models/Theme.ts: TypeScript interface for theme JSON structure
    - frontend/src/assets/worlds/village/themes/default.json: Day theme configuration
    - frontend/src/assets/worlds/village/themes/night.json: Night theme with dark overlay, lamp lights, lit windows
  key_links:
    - ThemeEngine integrates with OverworldScene via applyTheme() method called on scene start and theme change
    - Day/night detection uses JavaScript Date.getHours() to determine local time
    - Light system uses Phaser.GameObjects.Light for lamp cones
---

<objective>
Build the theme engine core and implement day/night cycle with automatic time detection.
Purpose: Enable the game world to visually adapt based on time of day with controllable light sources and window states.
Output: Working ThemeEngine.ts that loads theme JSON, applies camera tints, controls lamp lights and window tiles, and provides runtime theme switching with day/night auto-detection.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-game-world/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Theme model and default/night theme JSON files</name>
  <files>frontend/src/app/game/models/Theme.ts, frontend/src/assets/worlds/village/themes/default.json, frontend/src/assets/worlds/village/themes/night.json</files>
  <action>
  Create TypeScript interface for theme structure in Theme.ts:
  - id: string
  - name: string
  - palette: { sky?: string, overlay?: { color: string, alpha: number } }
  - lights: { lampTiles: number[], intensity: number, radius: number }
  - windowTiles: { day: number[], night: number[] }
  - particles?: { type: string, config: object }
  - decorationLayers?: string[]
  - audio?: { ambient?: string, music?: string }
  - npcOverrides?: { [npcId: string]: { dialogue: string } }

  Create default.json with:
  - id: "default", name: "Day"
  - palette.sky: light blue
  - No overlay
  - lights.lampTiles: empty array (no lamps emit at day)
  - windowTiles.day: array of tile indices for daytime windows

  Create night.json with:
  - id: "night", name: "Night"
  - palette.overlay: { color: "#1a1a3d", alpha: 0.4 } (dark blue tint)
  - lights.lampTiles: array of lamp tile indices that should emit light
  - lights.intensity: 0.8, radius: 100
  - windowTiles.night: array of lit window tile indices
  </action>
  <verify>
  - Verify Theme.ts interface compiles without errors
  - Check default.json and night.json are valid JSON
  - Confirm both files match the Theme interface structure
  </verify>
  <done>
  Theme.ts interface exists, default.json and night.json are valid and match interface
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ThemeEngine core with JSON loading and runtime switching</name>
  <files>frontend/src/app/game/systems/ThemeEngine.ts</files>
  <action>
  Create ThemeEngine.ts system with:
  - constructor(scene: Phaser.Scene, worldPackPath: string)
  - loadTheme(themeId: string): Promise<Theme> - fetches JSON from {worldPackPath}/themes/{themeId}.json
  - applyTheme(theme: Theme): void - applies all theme settings to scene
  - getCurrentTheme(): Theme - returns active theme
  - switchTheme(themeId: string): void - loads and applies new theme at runtime

  applyTheme should:
  - Apply camera color tint if overlay exists (scene.cameras.main.setTint())
  - Enable/disable Phaser light system based on lights configuration
  - For each lampTile index in theme.lights.lampTiles, add a light at that tile position
  - Swap window tiles from day to night variants (or vice versa) based on theme.windowTiles
  - Store current theme reference

  Include error handling for missing theme files and invalid JSON.
  </action>
  <verify>
  - Call loadTheme("default") and verify it returns valid Theme object
  - Call applyTheme() and check camera tint updates
  - Verify no console errors during theme loading/application
  </verify>
  <done>
  ThemeEngine.ts loads themes from JSON, applies camera tints, manages lights, and switches themes at runtime
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate day/night auto-detection and toggle UI in OverworldScene</name>
  <files>frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
  In OverworldScene.create():
  - Instantiate ThemeEngine: this.themeEngine = new ThemeEngine(this, 'assets/worlds/village')
  - Detect local time using JavaScript Date: const hour = new Date().getHours()
  - If hour >= 6 && hour < 18, load "default" theme, else load "night" theme
  - Apply the auto-detected theme: this.themeEngine.applyTheme(theme)

  Add day/night toggle button to game UI:
  - Create a UI button (text: "☀️/🌙") in top-right corner
  - On click, toggle between "default" and "night" theme
  - Call this.themeEngine.switchTheme(newThemeId)
  - Update button text to reflect current theme

  Store theme override preference in sessionStorage so manual toggle persists during session.
  </action>
  <verify>
  - Launch game during daytime hours (local time) - verify day theme loads
  - Manually set system time to 8 PM and reload - verify night theme loads
  - Click toggle button - verify theme switches immediately without reload
  - Verify lamp lights appear at night and windows change to lit variants
  - Verify camera overlay tint is visible in night mode
  </verify>
  <done>
  Day/night auto-detects from local time, theme toggle button works, theme switching happens at runtime without reload
  </done>
</task>

</tasks>

<verification>
1. Load game during day (6 AM - 6 PM local time) - default theme with no overlay or lamp lights
2. Load game at night (6 PM - 6 AM local time) - night theme with dark blue overlay, lamp lights emit cones, windows are lit
3. Click day/night toggle button - theme switches immediately, camera tint updates, lights appear/disappear
4. Check browser DevTools console - no errors during theme loading or application
5. Verify Theme.ts interface, default.json, and night.json all exist and are valid
</verification>

<success_criteria>
1. ThemeEngine.ts loads theme JSON from world pack themes/ directory
2. Camera applies dark blue overlay tint in night mode using Phaser pipeline
3. Lamp objects emit light cones at night using Phaser light system
4. Window tiles swap to lit variants at night
5. Day/night auto-detects from visitor's local time (6 AM - 6 PM = day, otherwise = night)
6. Toggle button in game UI allows manual theme override that persists in sessionStorage
</success_criteria>

<output>
After completion, create `.planning/phases/07-theme-engine/07-01-SUMMARY.md`
</output>
