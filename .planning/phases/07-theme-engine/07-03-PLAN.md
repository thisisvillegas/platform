---
phase: 07-theme-engine
plan: 03
type: execute
wave: 1
depends_on:
  - phase: 07-theme-engine
    plan: 01
    provides: ThemeEngine.ts with core theme loading and application
  - phase: 02-game-world
    provides: OverworldScene with audio management
  - phase: 05-pass-and-guest-auth
    provides: Auth0 protected dashboard routes
files_modified:
  - frontend/src/app/game/systems/ThemeEngine.ts
  - backend/src/routes/worldRoutes.ts
  - backend/src/models/WorldConfig.ts
  - frontend/src/app/dashboard/world-manager/world-manager.component.ts
  - frontend/src/app/dashboard/world-manager/world-manager.component.html
  - frontend/src/app/dashboard/world-manager/world-manager.component.scss
  - frontend/src/app/services/world-api.service.ts
  - backend/src/middleware/auth.ts
autonomous: true
must_haves:
  truths:
    - Ambient audio tracks crossfade on theme change
    - Day ambient uses birds and gentle music, night uses crickets and calm music
    - Seasonal themes have audio overrides defined in JSON
    - GET /api/world/theme returns active theme (public, no auth)
    - PUT /api/world/theme sets active theme (Auth0 protected, dashboard only)
    - MongoDB worldConfig collection stores active theme and override settings
    - World Manager dashboard page shows theme selector, override toggle, and theme preview
  artifacts:
    - backend/src/routes/worldRoutes.ts: GET /api/world/theme and PUT /api/world/theme endpoints
    - backend/src/models/WorldConfig.ts: MongoDB schema for worldConfig with activeTheme and themeMode fields
    - frontend/src/app/dashboard/world-manager/: Angular component for theme management
    - frontend/src/app/services/world-api.service.ts: Service with getActiveTheme() and setTheme() methods
  key_links:
    - ThemeEngine fetches active theme from GET /api/world/theme on scene start
    - Dashboard World Manager calls PUT /api/world/theme to persist theme changes
    - Audio crossfade uses Phaser sound manager with volume tweens
---

<objective>
Implement audio switching on theme change and build dashboard theme management with backend API persistence.
Purpose: Enable ambient audio to adapt to themes and give the dashboard owner full control over world theme with persistent storage.
Output: ThemeEngine with audio crossfade, backend theme API with MongoDB persistence, and dashboard World Manager component with theme controls.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-game-world/
@.planning/phases/07-theme-engine/07-01-PLAN.md
@.planning/phases/07-theme-engine/07-01-SUMMARY.md
@.planning/phases/07-theme-engine/07-02-PLAN.md
@.planning/phases/07-theme-engine/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ThemeEngine to manage audio crossfading</name>
  <files>frontend/src/app/game/systems/ThemeEngine.ts</files>
  <action>
  Extend ThemeEngine.ts to handle audio:

  Add private properties:
  - currentAmbientSound: Phaser.Sound.BaseSound | null
  - currentMusicSound: Phaser.Sound.BaseSound | null

  Update theme JSON structure in Theme.ts to include:
  - audio?: { ambient?: string, music?: string }

  Update applyTheme() to handle audio:
  - If theme.audio exists:
    - For ambient track:
      - If currentAmbientSound exists, fade out over 1000ms then stop
      - If theme.audio.ambient exists:
        - Load and play new ambient sound: scene.sound.add(theme.audio.ambient, { loop: true, volume: 0 })
        - Fade in over 1000ms to volume 0.6
    - Repeat same crossfade logic for music track with theme.audio.music

  Update default.json to include:
  - audio: { ambient: "assets/audio/day-ambient.mp3", music: "assets/audio/day-music.mp3" }

  Update night.json to include:
  - audio: { ambient: "assets/audio/night-ambient.mp3", music: "assets/audio/night-music.mp3" }

  Note: Audio files are placeholders. Actual audio asset integration happens in Phase 9.
  For now, ThemeEngine should handle missing audio files gracefully (log warning, continue without audio).
  </action>
  <verify>
  - Load day theme - verify ambient and music tracks start (or log warning if files missing)
  - Switch to night theme - verify day audio fades out over 1s, night audio fades in
  - Check console for smooth crossfade with no audio gaps or overlaps
  - Verify audio respects mute state if implemented
  </verify>
  <done>
  ThemeEngine crossfades ambient and music tracks on theme change with 1-second fade duration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create backend theme API with MongoDB persistence</name>
  <files>backend/src/routes/worldRoutes.ts, backend/src/models/WorldConfig.ts, backend/src/middleware/auth.ts</files>
  <action>
  Create WorldConfig.ts MongoDB model:
  ```typescript
  import mongoose from 'mongoose';

  const worldConfigSchema = new mongoose.Schema({
    activeTheme: { type: String, default: 'default' },
    themeMode: { type: String, enum: ['auto', 'manual'], default: 'auto' },
    updatedAt: { type: Date, default: Date.now }
  });

  export const WorldConfig = mongoose.model('WorldConfig', worldConfigSchema);
  ```

  Create worldRoutes.ts with:
  - GET /api/world/theme (public, no auth):
    - Query WorldConfig collection (single document)
    - If themeMode is 'auto', determine theme based on current server time (hour >= 6 && hour < 18 ? 'default' : 'night')
    - If themeMode is 'manual', return activeTheme from DB
    - Return JSON: { theme: string, mode: string }

  - PUT /api/world/theme (Auth0 protected):
    - Require auth middleware (check JWT from Auth0)
    - Accept body: { theme: string, mode?: 'auto' | 'manual' }
    - Validate theme is one of: 'default', 'night', 'valentine', 'christmas', 'autumn', 'halloween'
    - Update or create WorldConfig document: { activeTheme: theme, themeMode: mode || 'manual', updatedAt: Date.now() }
    - Return updated config

  Mount routes in backend Express app at /api/world
  </action>
  <verify>
  - Call GET /api/world/theme - returns valid theme and mode
  - Call PUT /api/world/theme with valid theme (with Auth0 token) - updates DB successfully
  - Call PUT without auth token - returns 401 Unauthorized
  - Verify WorldConfig document exists in MongoDB after first PUT
  - Test auto mode - returns different themes based on server time
  </verify>
  <done>
  Backend API provides GET /api/world/theme (public) and PUT /api/world/theme (protected), persisting to MongoDB WorldConfig collection
  </done>
</task>

<task type="auto">
  <name>Task 3: Build dashboard World Manager component with theme controls</name>
  <files>frontend/src/app/dashboard/world-manager/world-manager.component.ts, frontend/src/app/dashboard/world-manager/world-manager.component.html, frontend/src/app/dashboard/world-manager/world-manager.component.scss, frontend/src/app/services/world-api.service.ts</files>
  <action>
  Create world-api.service.ts:
  ```typescript
  import { Injectable } from '@angular/core';
  import { HttpClient } from '@angular/common/http';
  import { Observable } from 'rxjs';

  @Injectable({ providedIn: 'root' })
  export class WorldApiService {
    private apiUrl = '/api/world';

    constructor(private http: HttpClient) {}

    getActiveTheme(): Observable<{ theme: string; mode: string }> {
      return this.http.get<{ theme: string; mode: string }>(`${this.apiUrl}/theme`);
    }

    setTheme(theme: string, mode: 'auto' | 'manual'): Observable<any> {
      return this.http.put(`${this.apiUrl}/theme`, { theme, mode });
    }
  }
  ```

  Create world-manager.component.ts:
  - Inject WorldApiService
  - Load current theme on init: this.worldApi.getActiveTheme().subscribe()
  - Display theme selector dropdown with options: default, night, valentine, christmas, autumn, halloween
  - Display mode toggle: Auto (time-based) vs Manual (fixed theme)
  - On theme change: call worldApi.setTheme(selectedTheme, mode)
  - Show theme preview: small visual representation of theme (color swatch + particle icon)

  Create world-manager.component.html:
  - Sci-fi styled card with title "World Theme Manager"
  - Dropdown for theme selection
  - Toggle switch for auto/manual mode
  - Save button to apply changes
  - Preview section showing active theme info (name, overlay color, particles type)

  Create world-manager.component.scss:
  - Follow existing sci-fi "Minority Report" aesthetic from app.component.scss
  - Teal/cyan accents, dark backgrounds, glowing borders

  Add route to dashboard routing:
  - /dashboard/world-manager → WorldManagerComponent
  - Protect with Auth0 guard
  </action>
  <verify>
  - Navigate to /dashboard/world-manager (requires Auth0 login)
  - Verify current theme loads and displays correctly
  - Change theme in dropdown, click Save - verify PUT request sent
  - Reload dashboard - verify theme selection persists
  - Toggle auto mode - verify mode updates in backend
  - Visit game world in new browser tab - verify new theme is applied
  </verify>
  <done>
  Dashboard World Manager component loads, displays, and updates active theme with backend persistence
  </done>
</task>

</tasks>

<verification>
1. Load game world - verify it fetches theme from GET /api/world/theme and applies it
2. Open dashboard World Manager (Auth0 required) - verify current theme displays
3. Change theme to "valentine" in dashboard, save - verify PUT /api/world/theme called
4. Reload game world - verify valentine theme loads (pink tint, heart particles)
5. Switch theme to "night" in dashboard - verify crossfade from valentine to night audio
6. Set mode to "auto" - verify game world theme changes based on server time
7. Check MongoDB - verify worldConfig document exists with activeTheme and themeMode fields
</verification>

<success_criteria>
1. Ambient audio tracks crossfade smoothly on theme change (1-second fade)
2. Day theme uses birds/gentle music, night theme uses crickets/calm music (audio files are placeholders)
3. Seasonal themes have audio overrides defined in JSON
4. GET /api/world/theme returns active theme (public, no auth required)
5. PUT /api/world/theme sets active theme (Auth0 protected, dashboard only)
6. MongoDB worldConfig collection stores activeTheme and themeMode fields
7. World Manager dashboard page displays theme selector, auto/manual mode toggle, and theme preview
8. Theme changes in dashboard immediately reflect in game world on next load
</success_criteria>

<output>
After completion, create `.planning/phases/07-theme-engine/07-03-SUMMARY.md`
</output>
