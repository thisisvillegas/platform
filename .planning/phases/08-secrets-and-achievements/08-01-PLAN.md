---
phase: 08-secrets-and-achievements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/systems/SecretsManager.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/assets/worlds/village/secrets/collectibles.json
autonomous: true
must_haves:
  truths:
    - "10-15 glowing code fragment collectibles scattered across world map"
    - "Each collectible reveals real code snippet with explanation"
  artifacts:
    - "SecretsManager.ts system"
    - "collectibles.json with positions and code snippets"
  key_links: []
---

<objective>
Implement collectibles system with 10-15 glowing code fragment items scattered across the world map.
Purpose: Create discoverable easter eggs that reveal real code snippets from the portfolio, rewarding exploration.
Output: Working SecretsManager.ts that loads collectible positions from JSON, renders glowing items on map, detects pickup on collision, and reveals code snippet modals.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create collectibles data definition JSON</name>
  <files>frontend/src/assets/worlds/village/secrets/collectibles.json</files>
  <action>
    Create collectibles.json that defines 10-15 code fragment collectibles:
    - Each entry has: id (string), position {x, y} (world coordinates), codeSnippet (actual code), language (ts/html/scss/etc), explanation (what it does), title (short name)
    - Scatter positions across world map (not clustered, some hidden behind buildings/trees)
    - Use real code snippets from the platform project (Angular components, Phaser systems, Express routes, etc.)
    - Snippets should be 5-15 lines of meaningful, impressive code
    - Explanations should be 1-2 sentences describing the code's purpose
    - Examples:
      - Player movement system snippet
      - Dialogue system branching logic
      - Server stats real-time polling
      - Auth0 JWT validation
      - MongoDB aggregation query
      - Phaser camera follow implementation
  </action>
  <verify>collectibles.json exists with 10-15 entries, each has id/position/codeSnippet/language/explanation/title</verify>
  <done>collectibles.json created with diverse real code snippets from across the platform stack</done>
</task>

<task type="auto">
  <name>Task 2: Create SecretsManager system with collectible rendering and pickup</name>
  <files>frontend/src/app/game/systems/SecretsManager.ts</files>
  <action>
    Create SecretsManager.ts class that:
    - Loads collectibles from JSON file on init
    - Renders each collectible as a glowing pixel item on the map (use simple pulsing sprite or particle effect)
    - Tracks which collectibles have been picked up (stored in memory for now, persistence comes in 08-03)
    - Detects collision between player and collectible hitboxes
    - On collision: remove collectible sprite, add to collected array, emit pickup event
    - Provides methods: loadCollectibles(), renderCollectibles(scene), checkPickup(playerX, playerY), getCollected(), getTotalCount()
    - Collectible sprite: use simple glowing icon (placeholder: colored circle with pulse animation, or </> symbol if available)
    - Collision detection: simple distance check (player center within 16-24 pixels of collectible)
  </action>
  <verify>SecretsManager loads collectibles, renders them on map, detects pickup when player walks over them</verify>
  <done>SecretsManager.ts working with glowing collectibles that disappear on pickup and track collection state</done>
</task>

<task type="auto">
  <name>Task 3: Integrate SecretsManager into OverworldScene and show code snippet modal on pickup</name>
  <files>frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Integrate SecretsManager into OverworldScene:
    - Instantiate SecretsManager in scene create()
    - Call renderCollectibles() after map/player setup
    - In update loop, call checkPickup() with current player position
    - Listen for pickup event and show modal overlay with:
      - Collectible title
      - Code snippet in syntax-highlighted box (or plain text with monospace font for v1)
      - Language label
      - Explanation text
      - "Close" button or "Press SPACE to close"
    - Pause game input while modal is open
    - Modal styling: pixel-art border, dark semi-transparent background, sci-fi aesthetic
    - Play pickup sound effect (placeholder beep for now, audio comes in Phase 9)
  </action>
  <verify>Walking over a collectible opens modal showing code snippet and explanation, closing modal resumes game</verify>
  <done>Collectibles integrate into game world, pickup displays code modal with snippet and explanation</done>
</task>

</tasks>

<verification>
Load /world, walk around map to find glowing collectibles. Walk over one, verify modal appears with code snippet and explanation. Close modal, verify collectible is gone. Check multiple collectibles to confirm each shows different code.
</verification>

<success_criteria>
1. collectibles.json defines 10-15 unique code fragments with real project code
2. Glowing collectible sprites render at defined world positions
3. Walking over collectible triggers pickup (collision detection works)
4. Pickup displays modal with code snippet, language, and explanation
5. Picked collectibles disappear and don't respawn during session
6. Modal can be closed and game input resumes
</success_criteria>

<output>
After completion, create `.planning/phases/08-secrets-and-achievements/08-01-SUMMARY.md`
</output>
