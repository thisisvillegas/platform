---
phase: 08-secrets-and-achievements
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - frontend/src/app/game/systems/AchievementEngine.ts
  - frontend/src/app/game/ui/CollectiblesPanel.ts
  - frontend/src/app/game/ui/AchievementToast.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/assets/worlds/village/secrets/achievements.json
autonomous: true
must_haves:
  truths:
    - "9 pixel-art achievement badges unlock based on exploration behaviors"
    - "Tab opens collectibles panel showing found items, achievements, time spent"
    - "Konami code triggers visual effect and unlocks 'Old School' achievement"
  artifacts:
    - "AchievementEngine.ts system"
    - "CollectiblesPanel.ts overlay (Tab key)"
    - "achievements.json definition"
  key_links: []
---

<objective>
Implement achievement system with 9 unlockable badges, collectibles panel UI (Tab key), and Konami code easter egg.
Purpose: Reward visitor exploration with achievement unlocks, provide progress visibility through collectibles panel.
Output: Working achievement engine that tracks unlock conditions, shows toast notifications on unlock, and Tab-accessible panel displaying progress.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create achievement definitions and AchievementEngine system</name>
  <files>frontend/src/assets/worlds/village/secrets/achievements.json, frontend/src/app/game/systems/AchievementEngine.ts</files>
  <action>
    Create achievements.json defining 9 achievements (v1 subset):
    - source_diver: "Source Diver" - Found all code fragments (condition: collected all collectibles)
    - explorer: "Explorer" - Visited every building (condition: entered all 10 buildings)
    - social: "Social Butterfly" - Talked to every NPC (condition: interacted with all NPCs)
    - night_owl: "Night Owl" - Explored at night (condition: visited world during night theme)
    - konami: "Old School" - Entered Konami code (condition: ↑↑↓↓←→←→BA sequence)
    - curious: "Curious" - Opened collectibles panel (condition: pressed Tab once)
    - first_steps: "First Steps" - Entered first building (condition: any building entered)
    - chatterbox: "Chatterbox" - Had 10+ conversations (condition: dialogue count >= 10)
    - dedicated: "Dedicated" - Spent 30+ minutes in world (condition: timeSpent >= 1800 seconds)

    Create AchievementEngine.ts that:
    - Loads achievement definitions from JSON
    - Tracks unlock conditions per achievement
    - Exposes checkUnlock(achievementId, currentState) method
    - Emits unlock event when achievement is earned
    - Prevents duplicate unlocks (check unlocked array before emitting)
    - Stores unlocked achievements in memory (persistence in 08-03)
    - Provides methods: loadAchievements(), checkUnlock(id, state), getUnlocked(), getAll()
  </action>
  <verify>achievements.json has 9 entries, AchievementEngine loads them and can check unlock conditions</verify>
  <done>Achievement definitions exist and AchievementEngine tracks unlocks without duplicates</done>
</task>

<task type="auto">
  <name>Task 2: Create achievement unlock toast notification UI</name>
  <files>frontend/src/app/game/ui/AchievementToast.ts</files>
  <action>
    Create AchievementToast.ts UI component:
    - Renders pixel-art styled toast popup when achievement unlocks
    - Shows: achievement badge icon (simple placeholder sprite), achievement name, description
    - Appears at top-center of screen, slides down with animation
    - Stays visible for 4-5 seconds, then fades out
    - Play unlock sound effect (placeholder beep, audio in Phase 9)
    - Queue multiple toasts if several unlock at once (show one at a time)
    - Styling: pixel-art border, semi-transparent background, gold/yellow accent color
  </action>
  <verify>Achievement unlock triggers toast that animates in, displays name and description, then fades out</verify>
  <done>AchievementToast shows pixel-art notification when achievements unlock</done>
</task>

<task type="auto">
  <name>Task 3: Create CollectiblesPanel overlay (Tab key) with progress display</name>
  <files>frontend/src/app/game/ui/CollectiblesPanel.ts</files>
  <action>
    Create CollectiblesPanel.ts overlay:
    - Toggle visibility with Tab key
    - Full-screen semi-transparent dark overlay with centered panel
    - Display sections:
      1. Collectibles: "X/Y Code Fragments Found" with list of collected items (titles only)
      2. Achievements: Grid of unlocked achievement badges with names
      3. Exploration Stats: Buildings visited (X/10), NPCs talked to (X/Y), Time spent (formatted minutes:seconds)
    - Close on Tab key again or Escape key
    - Pause game input while panel is open
    - Styling: pixel-art borders, sci-fi aesthetic, scrollable if content overflows
    - Achievement badges: show locked badges as grayed/silhouetted, unlocked as colored
    - List collected code snippets by title (don't repeat full code, just titles)
  </action>
  <verify>Pressing Tab opens panel showing collectibles, achievements, and stats; Tab/Escape closes it</verify>
  <done>CollectiblesPanel overlay displays complete progress and toggles with Tab key</done>
</task>

<task type="auto">
  <name>Task 4: Integrate achievement system and Konami code into OverworldScene</name>
  <files>frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Integrate AchievementEngine, AchievementToast, and CollectiblesPanel into OverworldScene:
    - Instantiate AchievementEngine in scene create()
    - Track player state for achievements: buildingsVisited[], npcsInteracted[], dialogueCount, timeSpent
    - Check unlock conditions after relevant events:
      - Collectible pickup → check source_diver
      - Building entry → check explorer, first_steps
      - NPC dialogue end → check social, chatterbox
      - Tab press → check curious
      - Time update → check dedicated
    - Implement Konami code detection:
      - Listen for arrow key sequence: ↑↑↓↓←→←→BA (B and A can be any keys, e.g., B and A or Space and Enter)
      - On correct sequence: trigger visual effect (screen flash, particle burst, camera shake)
      - Unlock konami achievement
    - On achievement unlock event: show AchievementToast
    - Add Tab key listener to toggle CollectiblesPanel
    - Pass current state to CollectiblesPanel: collected items, unlocked achievements, stats
  </action>
  <verify>Achievements unlock on correct triggers, Konami code triggers effect and unlocks Old School, Tab opens panel</verify>
  <done>Achievement system fully integrated, Konami code works, collectibles panel shows live progress</done>
</task>

</tasks>

<verification>
Test achievement unlocks: collect all fragments (Source Diver), visit all buildings (Explorer), enter Konami code (Old School), press Tab (Curious). Verify toast appears on unlock. Open collectibles panel with Tab, verify all progress displays correctly. Konami code should trigger visual effect.
</verification>

<success_criteria>
1. 9 achievements defined in JSON with unlock conditions
2. AchievementEngine checks conditions and unlocks achievements without duplicates
3. Achievement toast notification appears on unlock with name and description
4. Collectibles panel opens/closes with Tab key
5. Panel shows collectibles found (X/Y), unlocked achievements, and exploration stats
6. Konami code (↑↑↓↓←→←→BA) triggers visual effect and unlocks "Old School" achievement
7. All unlock conditions trigger correctly (fragments, buildings, NPCs, time, Tab press)
</success_criteria>

<output>
After completion, create `.planning/phases/08-secrets-and-achievements/08-02-SUMMARY.md`
</output>
