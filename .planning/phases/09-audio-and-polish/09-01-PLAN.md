---
phase: 09-audio-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/systems/AudioManager.ts
  - frontend/src/app/game/scenes/OverworldScene.ts
  - frontend/src/app/game/ui/AudioControlsUI.ts
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Implement complete audio system with ambient background music, sound effects for game interactions, and mute/unmute controls that respect browser autoplay policies.
Purpose: Create an immersive audio experience for visitors while giving them full control and respecting browser autoplay restrictions.
Output: Working AudioManager with ambient music, SFX for doors/NPCs/collectibles, UI mute toggle, and autoplay-compliant initialization.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AudioManager system with ambient music</name>
  <files>frontend/src/app/game/systems/AudioManager.ts</files>
  <action>
    Create AudioManager.ts that:
    - Loads ambient background music (placeholder path: assets/worlds/village/audio/ambient.mp3)
    - Implements autoplay policy compliance (starts muted on first visit)
    - Provides playMusic(), pauseMusic(), stopMusic() methods
    - Handles music looping with configurable volume (default 0.3)
    - Stores mute state in localStorage (key: 'world-audio-muted')
    - Provides toggleMute() method that affects all audio
    - Emits events when mute state changes
    - Uses Phaser's sound system for web audio API handling
  </action>
  <verify>Background music plays when unmuted, loops continuously, and respects saved mute preference across sessions</verify>
  <done>AudioManager plays ambient music with proper looping, volume control, and mute state persistence</done>
</task>

<task type="auto">
  <name>Task 2: Add sound effects for game interactions</name>
  <files>frontend/src/app/game/systems/AudioManager.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Extend AudioManager to:
    - Load SFX files (door-enter.wav, npc-talk.wav, collectible-pickup.wav from assets/worlds/village/audio/)
    - Provide playSFX(soundKey: string, volume?: number) method
    - Play sound effects at configurable volume (default 0.5)
    - Respect global mute state for all SFX
    - Integrate with OverworldScene to trigger:
      * door-enter when player enters building
      * npc-talk when dialogue starts
      * collectible-pickup when collecting code fragments
    - Ensure SFX don't overlap inappropriately
  </action>
  <verify>Door entry, NPC interaction, and collectible pickup each play distinct sound effects, all respect mute toggle</verify>
  <done>Sound effects play for all three interaction types and honor mute state</done>
</task>

<task type="auto">
  <name>Task 3: Create mute/unmute UI toggle</name>
  <files>frontend/src/app/game/ui/AudioControlsUI.ts, frontend/src/app/game/scenes/OverworldScene.ts</files>
  <action>
    Create AudioControlsUI.ts that:
    - Renders a mute/unmute button in top-right corner of game canvas
    - Shows speaker icon with slash when muted, without slash when unmuted
    - Handles click/tap to toggle mute state via AudioManager
    - Updates icon immediately when state changes
    - Stays visible at all times during gameplay
    - Uses pixel-art style consistent with game aesthetic
    - Integrate with OverworldScene to render and position correctly
    - Show initial "Click to unmute" prompt on first visit (browser autoplay compliance)
  </action>
  <verify>Mute button visible in-game, clicking toggles audio on/off, icon updates correctly, first-visit prompt appears</verify>
  <done>Mute toggle UI renders, functions correctly, and prompts visitor to unmute on first visit</done>
</task>

</tasks>

<verification>
Test audio system end-to-end: Visit world for first time (should start muted with unmute prompt), click unmute (music plays), interact with door/NPC/collectible (hear SFX), toggle mute button (all audio stops), refresh page (mute state persists).
</verification>

<success_criteria>
1. Ambient background music plays and loops while exploring
2. Distinct sound effects play for door entry, NPC interaction, collectible pickup
3. Mute/unmute toggle button is visible and functional at all times
4. Audio starts muted on first visit with clear unmute prompt
5. Mute state persists across browser sessions via localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/09-audio-and-polish/09-01-SUMMARY.md`
</output>
