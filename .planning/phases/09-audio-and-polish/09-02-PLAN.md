---
phase: 09-audio-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/game/scenes/BootScene.ts
  - frontend/src/app/game/config/game.config.ts
  - frontend/angular.json
autonomous: true
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

<objective>
Optimize game loading performance and asset delivery to achieve sub-3-second initial load on 4G connections.
Purpose: Ensure fast, smooth game startup for visitors on mobile and slower connections.
Output: BootScene with progress bar, compressed assets, lazy loading for interiors, and verified bundle optimization.
</objective>

<execution_context>
@/Users/andres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loading progress bar to BootScene</name>
  <files>frontend/src/app/game/scenes/BootScene.ts</files>
  <action>
    Extend BootScene to:
    - Display loading progress bar during asset loading
    - Show percentage complete (0-100%) as text
    - Render progress bar with pixel-art styling matching game aesthetic
    - Center on canvas with game title or "Loading..." text above
    - Update smoothly as assets load
    - Fade out and transition to OverworldScene when complete
    - Handle load errors gracefully with retry option
  </action>
  <verify>Loading screen appears when game starts, progress bar fills smoothly, transitions to world when done</verify>
  <done>BootScene shows working progress bar with percentage that updates during asset load</done>
</task>

<task type="auto">
  <name>Task 2: Compress tileset and audio assets</name>
  <files>frontend/angular.json</files>
  <action>
    Optimize game assets:
    - Add build-time compression for PNG tilesets using imagemin or similar
    - Convert audio files to compressed formats (MP3 for music, OGG for SFX as fallback)
    - Configure angular.json to include asset optimization in production builds
    - Target: tileset PNGs under 500KB total, audio under 1MB total
    - Document asset compression process in comments
    - Ensure compressed assets maintain visual/audio quality
  </action>
  <verify>Production build shows smaller asset sizes in dist/ folder, game still renders correctly</verify>
  <done>Assets are compressed, production bundle size reduced, no visual/audio degradation</done>
</task>

<task type="auto">
  <name>Task 3: Implement lazy loading for interior maps</name>
  <files>frontend/src/app/game/scenes/OverworldScene.ts, frontend/src/app/game/config/game.config.ts</files>
  <action>
    Add lazy loading for building interiors:
    - Modify game.config.ts to separate exterior and interior asset lists
    - Update OverworldScene to only load exterior world assets on init
    - Implement loadInterior(buildingKey: string) method that:
      * Checks if interior assets already loaded
      * Loads interior tilemap JSON and associated sprites on-demand
      * Shows brief loading indicator if needed
      * Caches loaded interiors to avoid re-downloads
    - Hook into building entry system to trigger interior load before transition
    - Preload next likely interior when player approaches door (predictive)
  </action>
  <verify>Initial game load only downloads exterior assets, entering building triggers interior load, subsequent entries use cache</verify>
  <done>Interior maps load on-demand, initial load time reduced, re-entering buildings is instant</done>
</task>

<task type="auto">
  <name>Task 4: Verify Phaser bundle code-splitting</name>
  <files>frontend/angular.json</files>
  <action>
    Verify and optimize Angular bundle strategy:
    - Check that Phaser module is lazy-loaded (not in main.js bundle)
    - Confirm game route uses loadChildren for code-splitting
    - Verify in Network tab that visiting /dashboard does NOT download Phaser chunk
    - Add optimization flags to angular.json production config if needed
    - Document bundle splitting strategy in comments
    - Target: /dashboard main bundle under 500KB, game chunk separate
  </action>
  <verify>Network tab shows Phaser chunk only loads when visiting /world route, not on /dashboard</verify>
  <done>Phaser code-split verified, dashboard and game have separate bundles</done>
</task>

</tasks>

<verification>
Run production build, test initial load time on throttled 4G connection (Chrome DevTools), verify under 3 seconds. Check Network tab to confirm lazy loading works. Test on mobile device for real-world performance.
</verification>

<success_criteria>
1. BootScene displays progress bar during asset loading
2. Tileset PNGs and audio files are compressed for production
3. Interior maps lazy-load only when entering buildings
4. Phaser bundle is code-split and doesn't load on /dashboard
5. Initial game load completes in under 3 seconds on 4G connection
</success_criteria>

<output>
After completion, create `.planning/phases/09-audio-and-polish/09-02-SUMMARY.md`
</output>
